export interface FAQItem {
  question: string;
  answer: string;
}

export interface BonusItem {
  title: string;
  subtitle?: string;
  description: string;
  imageSrc: string;
}

export interface TestimonialImage {
  src: string;
  alt: string;
}